/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;
//I made this custom window editor but it's no longer neccesary. I kept it here in case I ever need to make another editor

[CustomEditor(typeof(ButtonScaler))]

public class ButtonScalerEditor : Editor
{
    public override void OnInspectorGUI()
    {
        //base.OnInspectorGUI();
        ButtonScaler buttonScaler = (ButtonScaler)target;
        buttonScaler.hoverScale = EditorGUILayout.Vector3Field("Button Hover Scale", buttonScaler.hoverScale);
        buttonScaler.desiredDuration = EditorGUILayout.FloatField("Scale Duration", buttonScaler.desiredDuration);
    }
}*/
